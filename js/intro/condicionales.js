if (condicion) {
    // se ejecuta si la condición se cumple
} else {
    // se ejecuta si la condición no se cumple
}

condicion ? 'si se cumple la condicion me ejecuto' : 'si no se cumple la condicion me ejecuto';

// falsy values
false, 0, -0, '', null, undefined, NaN 'Not a Number'

// coertion
!!0 // false
!!null // false

! // Not

!0 // true